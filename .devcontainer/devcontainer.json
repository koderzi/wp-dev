{
	"name": "wp-dev",
	"dockerComposeFile": "docker-compose.yaml",
	"service": "wordpress",
	"workspaceFolder": "/",
	"shutdownAction": "stopCompose",
	"customizations": {
		"vscode": {
			"settings": {
				"php.validate.executablePath": "/usr/local/bin/php"
			},
			"extensions": [
				"eamodio.gitlens"
			]
		}
	},
	"postCreateCommand": "dir=$(find /workspaces -maxdepth 1 -type d | grep -v '^/workspaces$' | head -n 1 | xargs basename) ; ln -s '/workspaces/$dir' '/var/www/html/wp-content/plugins/$dir' ; apt-get update && apt-get install -y git"
}
